
## Build and Verification
- ALWAYS run `npm run build` to verify that there are no compilation or JSX syntax errors locally before running `git push`. Never push code without checking the build first.
